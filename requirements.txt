# Dependencies needed by the required tests and the submitted benchmark.
pytest==9.1.1
python-dotenv==1.2.2
google-genai==2.23.0

# Other embedding backends remain optional.
#   Local, free, no API key : pip install -r requirements-local.txt
#   OpenAI                  : pip install openai
# See README.md "Tuy Chon Mo Hinh Nhung" or Phu luc B in the lab doc.
